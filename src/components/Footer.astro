---
import contactContent from '../data/site/contact.json';
import footerContent from '../data/site/footer.json';
import DonationSection from './DonationSection.astro';

const emailLink = `mailto:${contactContent.email}`;
const phoneLink = `tel:${contactContent.phoneDial}`;
---

<footer id="contact" class="border-t border-slate-900/10 bg-slate-950 py-14 text-slate-200">
	<div class="mx-auto max-w-6xl px-6">
		<!-- Donation Section -->
		<DonationSection />

		<div class="grid gap-8 md:grid-cols-3">
			<div>
				<h3 class="text-sm font-semibold uppercase tracking-[0.2em] text-slate-400">Contact Us</h3>
				<p class="mt-2 text-sm flex flex-col gap-2">
					<a class="text-slate-100 underline decoration-slate-500 underline-offset-4 transition hover:text-white" href={emailLink}>{contactContent.email}</a>
					<a class="text-slate-100 underline decoration-slate-500 underline-offset-4 transition hover:text-white" href={phoneLink}>{contactContent.phoneDisplay}</a>
				</p>
			</div>
			<div>
				<h3 class="text-sm font-semibold uppercase tracking-[0.2em] text-slate-400">Leadership</h3>
				{footerContent.leadership.map((person, index) => (
					<p class:list={["text-sm text-slate-300", index === 0 ? "mt-4" : "mt-2"]}>{person.name} - {person.role}</p>
				))}
			</div>
		</div>
		<div class="mt-10 border-t border-slate-800 pt-6">
			<h3 class="text-sm font-semibold uppercase tracking-[0.2em] text-slate-400">Supported By</h3>
			<div class="mt-4 flex w-full flex-row flex-wrap items-center justify-around gap-4">
				{footerContent.supporters.map((supporter) => (
					supporter.href ? (
						<a href={supporter.href}>
							<img src={supporter.image} alt={supporter.name} class="h-16 w-28 object-contain rounded-xl border border-slate-800 bg-white p-2" />
						</a>
					) : (
						<img src={supporter.image} alt={supporter.name} class="h-16 w-28 object-contain rounded-xl border border-slate-800 bg-white p-2" />
					)
				))}
			</div>
		</div>
		<div class="mt-10 border-t border-slate-800 pt-6 text-center text-xs text-slate-400">
			<p>&copy; {new Date().getFullYear()} Upper Hand International. All rights reserved. 501(c) Non-Profit Organization</p>
			<p class="mt-2">Site made by <a class="text-slate-200 underline decoration-slate-600 underline-offset-4 transition hover:text-white" href="https://betterstarter.dev" target="_blank" rel="noopener noreferrer">BetterStarter.dev</a></p>
		</div>
	</div>
</footer>

